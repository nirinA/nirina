<%- include('head', {title: 'login'}); -%>
<%- include('header'); -%>
<%- include('homebanner'); -%>
<%- include('banner'); -%>

<div id="maindisplay">
<h1><%= title %></h1>
<!-- login / register are temporarily unavailable -->
       <!-- show any messages that come back with authentication -->
        <% if (message.length > 0) { %>
      <div class="error" style="background-color: red;"><%= message %></div>
      <% } %>
      <br />
   <form action="/login/password" method="post">   
      <label for="username">Username</label><br />
      <input type="text" name="username" required /><br />
      <label for="password">Password</label><br />
      <input type="password" name="password" required /><br />
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="submit" value="login" /><br />
    </form>
</div>
<script>
$( () => {
    $("#maindisplay").prepend('<div id="datebox"></div>');
    FormatDate.FullClock();
    $('<div id="fortune_text"></div>').insertAfter("#datebox");
    //$("#fortune_text").load("https://apod.nasa.gov/apod.rss");

/*
$.ajax(
    {
        dataType: "json",
	//url: "https://apod.nasa.gov/apod.rss", //> CORS ERROR
        url: "https://helloacm.com/api/fortune/", 
        cache: false,
	//headers: {
        //'Content-Type': 'application/x-www-form-urlencoded'
	//},
	//type: "POST", // or type:"GET" or type:"PUT" 
        success: function (response) {
	  $("#fortune_text").html(response);
        },
	//error: function () {
        //  $("#fortune_text").html("Error: CORS");
	//}
    })
   */
});

</script>

<script>$(".<%= title %>").addClass("noaction");</script>
<%- include('footer'); -%>
